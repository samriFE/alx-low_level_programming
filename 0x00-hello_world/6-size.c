#include <stdio.h>
/**
*
*main -Entry point
*
* Return: Always 0 (success)
*/
int main(void)
{
printf("size of a char : %d byte(s)/n",sizeof(char);
printf(sizeof(int));
printf(sizeof(iong int));
printf(sizeoflong long int));
return (0);
}
